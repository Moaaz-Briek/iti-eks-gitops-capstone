name: Build Frontend Image [ Testing ]

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'

jobs:
  frontend:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Print Hello World
      run: echo "Hello World!"
